# MCP Servers

MCP Servers manages upstream Model Context Protocol servers that `cgm` can connect to and expose through its own MCP surface.

Use **Topics** for detailed documentation of every editor field, JSON creation, OAuth, tool exposure, and lifecycle action.
