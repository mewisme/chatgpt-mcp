name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    name: Change scope
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      full: ${{ steps.scope.outputs.full }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Classify changes
        id: scope
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          BEFORE_SHA: ${{ github.event.before }}
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          PUSH_HEAD_SHA: ${{ github.sha }}
        run: |
          if [[ "${EVENT_NAME}" == "pull_request" ]]; then
            base="${PR_BASE_SHA}"
            head="${PR_HEAD_SHA}"
          else
            base="${BEFORE_SHA}"
            head="${PUSH_HEAD_SHA}"
          fi

          if [[ -z "${base}" || "${base}" =~ ^0+$ ]] || ! git cat-file -e "${base}^{commit}" || ! git cat-file -e "${head}^{commit}"; then
            echo "full=true" >> "${GITHUB_OUTPUT}"
            echo "No usable comparison range; running full CI."
            exit 0
          fi

          mapfile -t files < <(git diff --name-only "${base}" "${head}")
          full=false
          for file in "${files[@]}"; do
            case "${file}" in
              README.md|CONTRIBUTING.md|CODE_OF_CONDUCT.md|SECURITY.md|docs/*) ;;
              *) full=true; break ;;
            esac
          done
          echo "full=${full}" >> "${GITHUB_OUTPUT}"
          printf 'Changed files (%d):\n' "${#files[@]}"
          printf '  %s\n' "${files[@]}"
          echo "Full CI: ${full}"

  admin-ui:
    name: Admin UI checks
    runs-on: ubuntu-latest
    needs:
      - changes
    timeout-minutes: 10
    defaults:
      run:
        working-directory: plugins/admin-ui
    steps:
      - name: Skip heavy checks
        if: needs.changes.outputs.full != 'true'
        working-directory: ${{ github.workspace }}
        run: echo "Documentation-only change; Admin UI checks skipped."

      - name: Checkout
        if: needs.changes.outputs.full == 'true'
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup pnpm
        if: needs.changes.outputs.full == 'true'
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10
        with:
          version: 11

      - name: Setup Node
        if: needs.changes.outputs.full == 'true'
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: plugins/admin-ui/pnpm-lock.yaml

      - name: Install
        if: needs.changes.outputs.full == 'true'
        run: pnpm install --frozen-lockfile

      - name: Audit production dependencies
        if: needs.changes.outputs.full == 'true'
        run: pnpm audit --prod

      - name: Test with coverage
        if: needs.changes.outputs.full == 'true'
        run: pnpm test:coverage

      - name: Lint
        if: needs.changes.outputs.full == 'true'
        run: pnpm lint

      - name: Typecheck
        if: needs.changes.outputs.full == 'true'
        run: pnpm typecheck

      - name: Build
        if: needs.changes.outputs.full == 'true'
        run: pnpm build

  security:
    name: Security checks
    runs-on: ubuntu-latest
    needs:
      - changes
    timeout-minutes: 15
    steps:
      - name: Skip heavy checks
        if: needs.changes.outputs.full != 'true'
        run: echo "Documentation-only change; security checks skipped."

      - name: Checkout
        if: needs.changes.outputs.full == 'true'
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Go
        if: needs.changes.outputs.full == 'true'
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
          cache: true

      - name: Setup Node
        if: needs.changes.outputs.full == 'true'
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24

      - name: Install security tooling
        if: needs.changes.outputs.full == 'true'
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@v1.7.0
          go install github.com/securego/gosec/v2/cmd/gosec@v2.29.0
          go install honnef.co/go/tools/cmd/staticcheck@v0.8.1
          go install github.com/rhysd/actionlint/cmd/actionlint@v1.7.12

      - name: Vulnerability scan
        if: needs.changes.outputs.full == 'true'
        run: govulncheck ./...

      - name: Verify tidy module metadata
        if: needs.changes.outputs.full == 'true'
        run: go mod tidy -diff

      - name: Verify modules
        if: needs.changes.outputs.full == 'true'
        run: go mod verify

      - name: Static analysis
        if: needs.changes.outputs.full == 'true'
        run: ./scripts/staticcheck.sh

      - name: Security analysis
        if: needs.changes.outputs.full == 'true'
        run: |
          gosec -quiet -fmt=json -exclude-dir=pkg/cloudflared ./... > "${RUNNER_TEMP}/gosec.json" || true
          node scripts/check-gosec-baseline.mjs "${RUNNER_TEMP}/gosec.json" scripts/gosec-baseline.json

      - name: Go coverage gate
        if: needs.changes.outputs.full == 'true'
        env:
          CHATGPT_MCP_CONFIG_DIR: ${{ runner.temp }}/chatgpt-mcp-coverage-test
        run: |
          go test -count=1 -coverprofile="${RUNNER_TEMP}/coverage.out" ./...
          coverage=$(go tool cover -func="${RUNNER_TEMP}/coverage.out" | awk '/^total:/ {gsub(/%/, "", $3); print $3}')
          echo "Go coverage: ${coverage}%"
          awk -v coverage="${coverage}" 'BEGIN { if (coverage + 0 < 66.0) { print "Go coverage must be at least 66.0%" > "/dev/stderr"; exit 1 } }'

      - name: Plugin workflow definitions
        if: needs.changes.outputs.full == 'true'
        run: |
          node scripts/plugin-workflow.mjs validate
          node --test scripts/plugin-workflow.test.mjs

      - name: Workflow lint
        if: needs.changes.outputs.full == 'true'
        run: actionlint .github/workflows/ci.yml .github/workflows/release.yml .github/workflows/plugins.yml

      - name: Installer lint
        if: needs.changes.outputs.full == 'true'
        run: shellcheck install.sh

  race:
    name: Race test
    runs-on: ubuntu-latest
    needs:
      - changes
    timeout-minutes: 10
    steps:
      - name: Skip heavy checks
        if: needs.changes.outputs.full != 'true'
        run: echo "Documentation-only change; race test skipped."

      - name: Checkout
        if: needs.changes.outputs.full == 'true'
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Go
        if: needs.changes.outputs.full == 'true'
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
          cache: true

      - name: Test with race detector
        if: needs.changes.outputs.full == 'true'
        env:
          CHATGPT_MCP_CONFIG_DIR: ${{ runner.temp }}/chatgpt-mcp-go-race-test
        run: go test -count=1 -race ./...

  native:
    name: Native ${{ matrix.name }}
    runs-on: ${{ matrix.runner }}
    needs:
      - changes
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux
            runner: ubuntu-latest
            binary: chatgpt-mcp
          - name: Windows
            runner: windows-latest
            binary: chatgpt-mcp.exe
          - name: macOS
            runner: macos-latest
            binary: chatgpt-mcp
    steps:
      - name: Skip heavy checks
        if: needs.changes.outputs.full != 'true'
        run: echo "Documentation-only change; native checks skipped."

      - name: Checkout
        if: needs.changes.outputs.full == 'true'
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Go
        if: needs.changes.outputs.full == 'true'
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
          cache: true

      - name: Setup Node
        if: needs.changes.outputs.full == 'true'
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24

      - name: Validate Unix installer
        if: needs.changes.outputs.full == 'true' && runner.os != 'Windows'
        run: sh -n install.sh

      - name: Validate PowerShell installer
        if: needs.changes.outputs.full == 'true' && runner.os == 'Windows'
        shell: pwsh
        run: ./scripts/test-install-ps1.ps1

      - name: Validate Windows PowerShell installer
        if: needs.changes.outputs.full == 'true' && runner.os == 'Windows'
        shell: powershell
        run: ./scripts/test-install-ps1.ps1

      - name: Local install smoke
        if: needs.changes.outputs.full == 'true'
        run: node scripts/install-local.mjs

      - name: Package alias smoke
        if: needs.changes.outputs.full == 'true'
        run: node --test scripts/prepare-package-aliases.test.mjs

      - name: Test
        if: needs.changes.outputs.full == 'true'
        env:
          CHATGPT_MCP_CONFIG_DIR: ${{ runner.temp }}/chatgpt-mcp-go-test
        run: go test -count=1 ./...

      - name: Vet
        if: needs.changes.outputs.full == 'true'
        run: go vet ./...

      - name: Build
        if: needs.changes.outputs.full == 'true'
        run: go build -trimpath -o ./dist-smoke/${{ matrix.binary }} ./

      - name: Runtime smoke
        if: needs.changes.outputs.full == 'true'
        run: node scripts/smoke-release.mjs ./dist-smoke/${{ matrix.binary }}

  cross-build:
    name: Cross build ${{ matrix.os }}/${{ matrix.arch }}
    runs-on: ubuntu-latest
    needs:
      - changes
    timeout-minutes: 10
    strategy:
      fail-fast: false
      max-parallel: 3
      matrix:
        include:
          - os: linux
            arch: amd64
          - os: linux
            arch: arm64
          - os: windows
            arch: amd64
          - os: windows
            arch: arm64
          - os: darwin
            arch: amd64
          - os: darwin
            arch: arm64
    steps:
      - name: Skip heavy checks
        if: needs.changes.outputs.full != 'true'
        run: echo "Documentation-only change; cross build skipped."

      - name: Checkout
        if: needs.changes.outputs.full == 'true'
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Go
        if: needs.changes.outputs.full == 'true'
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
          cache: true

      - name: Build
        if: needs.changes.outputs.full == 'true'
        env:
          GOOS: ${{ matrix.os }}
          GOARCH: ${{ matrix.arch }}
          CGO_ENABLED: 0
        run: go build -trimpath -ldflags "-s -w" -o chatgpt-mcp ./
